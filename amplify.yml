version: 1
frontend:
  buildpath: frontend
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    base-directory:
      - build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
